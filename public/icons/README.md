# TODO: Replace Placeholder Icons

These placeholder PNGs are temporary. Replace them with real artwork before release.

Required files (exact names):
- `icon16.png` (16x16)
- `icon32.png` (32x32)
- `icon48.png` (48x48)
- `icon128.png` (128x128)
- `favicon-16.png` (16x16)
- `favicon-32.png` (32x32)

Notes:
- The extension manifest uses `icon16.png`, `icon32.png`, `icon48.png`, and `icon128.png`.
- The new tab page favicon uses `favicon-32.png` with fallback `favicon-16.png`.
